<div><%= render 'users/userinfo' %></div>

<h1>Book detail</h1>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit "変更を保存", url: books_path, method: :patch %>
<% end %>