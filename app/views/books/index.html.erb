<div class="container">
  <div class="row">
    <div class="col-lg-4">
      <%= render 'users/userinfo' %>
    </div>

    <div class="col-lg-8">
      <h4>Books</h4>
      <table>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
      </tr>
      <tr>
      　<% @books.each do |books| %>
          <td><%= books.title %></td>
          <td><%= books.body %></td>
      　<% end %>
      </tr>
      </table>
    </div>
  </div>
</div>